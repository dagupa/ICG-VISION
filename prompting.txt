================================================================================
2026-07-16 (eliminar bloque redundante window.crimpingMasterData de app.js)
PROMPT:
Comprobar si los datos de crimpingMasterData estaban duplicados entre master-data.js
y app.js, y eliminar el bloque redundante.

ANÁLISIS:
- master-data.js: const crimpingMasterData = [...] → fuente real de datos (usada por app.js)
- app.js: window.crimpingMasterData = [...] → código muerto (asignado pero nunca leído)
  porque las referencias en app.js a `crimpingMasterData` resuelven al `const` global
  de master-data.js, no a la propiedad `window.crimpingMasterData`.
- Ambas copias eran idénticas en contenido.

SOLUCIÓN:
- Eliminado el bloque `// --- MASTER DATA CRIMPAR ---` + `window.crimpingMasterData = [...]`
  de js/app.js (~580 líneas, de la línea 93 a la 671).
- js/master-data.js queda como única fuente de verdad para los datos de crimpado.
- Para añadir nuevos terminales: editar solo master-data.js.

================================================================================
2026-07-16 (actualización de la ayuda con nuevas funcionalidades)
PROMPT:
Actualizar la ayuda con todas las nuevas funcionalidades.

SOLUCIÓN:
- index.html, sección 2 (Barra de herramientas):
  · Filtro por elemento y filtro por cable actualizados para mencionar el separador "+" (multi-valor).
- index.html, sección 4 (Tabla de conexiones):
  · Reescrita completamente: checkbox + personalizar columnas se mantienen.
  · Añadidos cuatro bloques de error (uno por tipo) con su color identificativo:
    naranja (dup), ámbar (sección), rojo (terminal incompat), morado (longitud).
  · Entrada unificada para el aviso en encabezados de columna (todos los tipos).
- index.html, sección 5 (Panel lateral):
  · Tarjeta DATOS actualizada para mencionar el apartado ESTADO y referenciar sección 10.
- index.html, nueva sección 10 (Control de calidad del fichero Excel):
  · Descripción general del análisis automático al cargar el fichero.
  · Cuatro entradas detalladas (duplicados, sección, terminal, longitud) con círculos de color.
  · Entrada para el indicador ESTADO: gris/verde/colores parpadeantes + tooltip.

================================================================================
2026-07-16 (tooltip enriquecido para todos los tipos de error en celdas)
PROMPT:
Quiero que al pasar el ratón por cualquier celda con error aparezca el mismo tooltip
enriquecido que muestra el error de terminal (tarjeta flotante con título, descripción
y "Toca para cerrar"), no solo el title HTML del navegador.

SOLUCIÓN:
- index.html: div 'termCompatTip' renombrado a 'cellErrorTip' con IDs en cada parte
  (cellErrorTipCard, cellErrorTipIcon, cellErrorTipTitle, cellErrorTipBody, cellErrorTipHint).
- js/app.js: 
  · Nueva función showCellErrorTip(el, scheme, titleText, bodyHtml): aplica dinámicamente
    el esquema de color (red/orange/amber/purple) a todos los elementos del tooltip.
  · Nueva función hideCellErrorTip(): oculta el tooltip genérico.
  · showTermCompatTip/hideTermCompatTip → thin wrappers que llaman a las funciones genéricas.
  · Iconos en render: hasMissingSeccion, hasNonNumLong, isDupCell usan onmouseenter/onmouseleave
    con showCellErrorTip/hideCellErrorTip en lugar de title="".

================================================================================
2026-07-16 (icono con tooltip en todas las celdas con error)
PROMPT:
En las celdas de los terminales cuando hay un error se añade un icono con tooltip explicativo.
Quiero que esto ocurra en todas las celdas con errores de las diferentes columnas.

SOLUCIÓN:
- js/app.js, renderTable() - celda:
  · Añadida variable `dupTooltip` con mensaje específico según columna (posición u orden).
  · isDupCell: nuevo icono `copy` naranja (text-orange-500) con title=dupTooltip.
  · isDupCell: añadido text-orange-600 dark:text-orange-400 al div contenedor.
  · hasIncompat, hasMissingSeccion, hasNonNumLong ya tenían icono — sin cambio.

================================================================================
2026-07-16 (parpadeo en celdas con error de terminal y longitud)
PROMPT:
Las celdas con errores en terminales o en la longitud también tienen que parpadear.

SOLUCIÓN:
- css/app.css: Añadidas dos nuevas animaciones de parpadeo:
  · incompatBlink + .cell-incompat-blink → parpadeo rojo (rgba 239,68,68)
  · nonNumBlink + .cell-nonnum-blink → parpadeo morado (rgba 168,85,247)
- js/app.js, renderTable() - celda:
  · hasIncompat: sustituido bg-red-50/dark:bg-red-900/10 por clase cell-incompat-blink
  · hasNonNumLong: sustituido bg-purple-50/dark:bg-purple-900/10 por clase cell-nonnum-blink

================================================================================
2026-07-16 (armonizar colores de errores en columnas con colores del ESTADO)
PROMPT:
Armonizar los colores de los errores en las columnas de la tabla con los definidos para el ESTADO.

SOLUCIÓN:
- css/app.css: 
  · cell-dup-blink → naranja (rgba 249,115,22)
  · cell-seccion-missing → ámbar (rgba 202,138,4)
- js/app.js, renderTable() - icono cabecera columna:
  · Añadida variable warnColorClass con color por tipo:
    rojo (terminal incompat), naranja (duplicados), morado (longitud), ámbar (sección)
- js/app.js, renderTable() - celda:
  · hasNonNumLong: bg-red → bg-purple-50/dark:bg-purple-900/10, text-purple-600, icono text-purple-500
  · hasMissingSeccion: icono text-red-600 → text-amber-600 dark:text-amber-400
  · hasIncompat: sin cambio (rojo)

================================================================================
2026-07-16 (colores diferenciados por tipo de error en ESTADO)
PROMPT:
Cambiar los colores de los círculos del apartado ESTADO:
1. Posiciones duplicadas → Naranja parpadeante
2. Órdenes duplicadas → Naranja parpadeante
3. Sección sin definir con terminal asignado → Amarillo parpadeante
4. Terminal incompatible con la sección → Rojo parpadeante
5. Longitud con valor no numérico → Morado parpadeante

SOLUCIÓN:
- js/app.js, updateEstadoUI(): cada error ahora lleva su clase CSS específica
  (estado-dot-orange, estado-dot-yellow, estado-dot-red, estado-dot-purple).
- css/app.css: sustituido el único @keyframes estadoDotBlink por 4 animaciones
  independientes (Orange, Yellow, Red, Purple) con su glow de color.
  Amarillo usa #ca8a04 (amber-600) en lugar de amarillo puro para garantizar
  contraste visible en modo claro y oscuro.

================================================================================
2026-07-16 (aparatado ESTADO en tarjeta Datos del sidebar)
PROMPT:
En la tarjeta "Datos" añadir justo debajo de la fecha un apartado "ESTADO" con círculos de colores.
Verde fijo: todo correcto. Rojo parpadeante: un círculo por cada tipo de error con tooltip al pasar el ratón.
Errores considerados: sección sin definir con terminal asignado, terminal incompatible con sección,
posiciones duplicadas, órdenes duplicadas, longitud no numérica.

SOLUCIÓN:
- js/app.js: Nuevos Sets globales `_seccionMissingSet` y `_termIncompatSet`.
  · _checkDuplicateColumns() los puebla y llama a `updateEstadoUI()` al final.
  · Nueva función `updateEstadoUI()`: muestra gris (sin datos), verde "Correcto" (sin errores)
    o círculos rojos parpadeantes con tooltip descriptivo (uno por tipo de error).
- index.html: Nuevo bloque `<div class="flex flex-col...">` con id="estado-circles" debajo
  de la fila Fecha/Plano dentro de la tarjeta DATOS del sidebar.
- css/app.css: Nueva keyframe `estadoDotBlink` + clase `.estado-dot-blink` (parpadeo con glow rojo).

================================================================================
2026-07-16 (centrar icono de error en celda)
PROMPT:
Actualmente cuando se detecta un error las celdas parpadean de color rojo y muestran un icono de advertencia justificado a la izquierda. Quiero que ese icono quede centrado en la celda.

SOLUCIÓN:
- js/app.js, renderizado de filas en Lista conexiones (~línea 3283):
  · Al div contenedor del icono/valor se le añade la clase `justify-center` condicionalmente cuando `hasMissingSeccion` es true.
  · Esto centra el icono `alert-circle` en las celdas de SECCIÓN vacías que tienen terminal asignado (caso más visible en la tabla).
  · El cambio es: `${hasMissingSeccion?'justify-center':''}` añadido a las clases del div flex.

================================================================================
2026-07-09 (eliminar filas con solo datos en A/B antes de cargar)
PROMPT:
Si las filas del Excel tienen datos en la columna A (posición) o B (orden)
pero ningún dato en el resto de columnas (C–P), esas filas deben eliminarse
antes de cargar el archivo.

SOLUCIÓN:
- js/app.js, handleFileUpload():
  · El .filter() al construir rawData se amplía: además de exigir posición
    numérica, se comprueba que al menos una de las columnas C–P tenga valor.
  · DATA_KEYS = [cod_cable, seccion, longitud, marcado, cable_marca,
    de_elemento, de_punto, de_terminal, de_manguito, para_manguito,
    para_elemento, para_punto, para_terminal, observaciones].
  · Si todas están vacías → fila descartada silenciosamente.
================================================================================
2026-07-09 (mensaje de carga ampliado con errores de sección y terminal incompatible)
PROMPT:
Al mensaje de alerta que aparece al cargar el fichero cuando se detectan
posiciones duplicadas, añadir también información sobre filas con sección
faltante (cuando hay terminal asignado) y filas con terminal incompatible
con la sección indicada.

SOLUCIÓN:
- js/app.js, _checkDuplicateColumns(data):
  · _tv(t): helper igual a _tvh — terminal con valor real (no vacío, no S/T,
    no S/M, no KN).
  · secMissingPos: posiciones de filas con sección vacía y terminal asignado.
  · termIncompatPos: posiciones de filas donde de_terminal o para_terminal
    no es compatible con la sección (via checkSectionCompatibility).
  · La condición de salida temprana incluye los 4 arrays.
  · _listPos(arr, max=10): muestra hasta 10 posiciones; si hay más, añade
    "y N más..." para no saturar el alert.
  · El mensaje muestra cada tipo de error como una línea con ●, incluyendo
    conteo y posiciones afectadas.
  · El alert se dispara si hay CUALQUIER tipo de incidencia, no solo duplicados.
================================================================================
2026-07-09 (warning clickable en cabeceras A, B y D; filtrado unificado para duplicados y sección faltante)
PROMPT:
Añadir icono de warning parpadeante en las cabeceras de las columnas A
(Posición) y B (Orden) cuando hay valores duplicados. Todos los iconos de
warning (A, B, D, J, O) deben funcionar igual: clic filtra la tabla por ese
tipo de error; segundo clic quita el filtro.

SOLUCIÓN:
- js/app.js, renderTable() — Pre-scan:
  · hasDupPosicion = _dupPosicions.size > 0
  · hasDupOrden    = _dupOrdenes.size > 0
  (los sets ya se calculan en _checkDuplicateColumns al cargar el fichero)
- js/app.js, renderTable() — Generación de cabecera:
  · Unificado: colHasTermErrors, colHasDupErrors, colHasSecErrors →
    colHasAnyError. Un único bloque genera el warnIcon con onclick
    toggleTerminalErrorFilter para todos los tipos.
  · isFiltered (antes isTermFiltered) aplica a cualquier columna.
  · Títulos diferenciados por tipo de error (term / dup / sección).
- js/app.js, renderTable() — Filtrado paso 2:
  · 'posicion' → filtra filas cuya posición está en _dupPosicions.
  · 'orden'    → filtra filas cuyo orden está en _dupOrdenes.
  · 'seccion'  → filtra filas con sección vacía y terminal asignado.
  · resto      → lógica original de incompatibilidad de sección.
================================================================================
2026-07-09 (warning en cabecera de columna Sección cuando falta sección con terminal asignado)
PROMPT:
Cuando exista el error de sección faltante con terminal asignado, mostrar
también el icono de warning parpadeante en el encabezado de la columna
"Sección" (igual que el que aparece en las columnas De Terminal / Para Terminal
cuando hay incompatibilidad de sección).

SOLUCIÓN:
- js/app.js, renderTable() — bloque Pre-scan:
  · Helper _tvh(t): reutiliza la misma lógica que _termHasVal en celdas.
  · hasSeccionMissingErrors: true si alguna fila del conjunto filtrado tiene
    seccion vacía y al menos uno de sus terminales tiene valor real.
  · En la generación del encabezado (warnIcon): si c.key === 'seccion' y
    hasSeccionMissingErrors, se añade el icono triangle-alert con la clase
    term-warn-blink y el tooltip "Hay filas con terminal asignado pero sin
    sección indicada" (sin clic — no hay filtro asociado a esta columna).
================================================================================
2026-07-09 (validación: sección obligatoria cuando hay terminal asignado)
PROMPT:
Añadir un nuevo control: si la columna J "De terminal" o la columna O
"Para terminal" tienen datos, es obligatorio que exista el dato de sección
en la columna D "Sección". Si falta, la celda de sección se marca en rojo
parpadeante.

SOLUCIÓN:
- css/app.css: nueva animación @keyframes sectionMissingBlink y clase
  .cell-seccion-missing (fondo rojo parpadeante, igual que .cell-dup-blink).
- js/app.js, renderTable() — bucle de celdas:
  · Helper local _termHasVal(t): devuelve true si el terminal tiene valor
    real (no vacío, no 'S/T', no 'S/M', no isKN).
  · hasMissingSeccion = c.key === 'seccion' && !v &&
    (_termHasVal(r.de_terminal) || _termHasVal(r.para_terminal)).
  · Si hasMissingSeccion: se aplica la clase 'cell-seccion-missing' al <td>
    y se muestra un icono data-lucide="alert-circle" rojo con tooltip
    "Sección obligatoria — hay terminal asignado".
================================================================================
2026-07-08 (vuelta a header:'A' + defval:'' para acceso robusto por columna)
PROMPT:
Con { header: 1 } las filas con celdas vacías podían devolver arrays
compactos, desplazando las posiciones. Se revierte a { header:'A', defval:'' }
que garantiza acceso por letra de columna Excel, y se aclara el mapeo de
los dos formatos con comentarios explícitos.

SOLUCIÓN:
- js/app.js, handleFileUpload():
  · XLSX.utils.sheet_to_json con { header:'A', defval:'' }:
    - 'A': columnas nombradas por letra Excel (A,B,C...) siempre por posición.
    - defval:'': celdas vacías dan '' en lugar de undefined (sin sparse arrays).
  · Mapeo de datos explícito con claves letra (row['C'], row['J'], etc.)
    idéntico para ambos formatos de Excel, con comentario de cada columna.
  · Detección multiHdr y mapeo de metadatos sin cambios.
  · Resumen cables: fallback a cable_marca (G) cuando cod_cable (C) está vacío.
================================================================================
2026-07-08 (acceso por índice en lugar de letra de columna)
PROMPT:
Las vistas de resumen seguían sin funcionar. El problema raíz era que
XLSX.utils.sheet_to_json con { header: "A" } puede usar el texto de la
primera fila como nombres de columna en lugar de letras fijas por posición,
dando nombres distintos según el tipo de encabezado del Excel.

SOLUCIÓN:
- js/app.js, handleFileUpload(): se cambia XLSX.utils.sheet_to_json a
  { header: 1 } que devuelve ARRAYS por fila. El acceso pasa a ser por
  índice numérico (0=A, 1=B, 2=C… 9=J, 10=K, 14=O, 23=X, 26=AA, etc.)
  garantizando mapeo por POSICIÓN absoluta independiente del contenido.
  Helpers añadidos: gc(ri,ci) para extraer una celda y _sv(v) para
  convertir a string seguramente.
  El resto de la lógica (_isNumPos, _multiHdr, masterMap) no cambia.
================================================================================
2026-07-08 (resumen cables vacío en Excel multi-fila)
PROMPT:
En Excel con encabezado de más de una fila, la vista "Resumen Cables" no
mostraba ningún dato porque la columna C (Cód. cable) está vacía en ese
formato; los identificadores de cable están en la columna G (Cable / nº).

SOLUCIÓN:
- js/app.js, renderSummary(), rama summary-cables:
  Se sustituye la búsqueda estricta de r.cod_cable por una clave con
  fallback: _ckey = cod_cable.trim() || cable_marca.trim().
  · Si cod_cable tiene valor (Excel de una sola cabecera): comportamiento
    idéntico al anterior.
  · Si cod_cable está vacío (Excel multi-fila): se agrupa por cable_marca
    (col. G), mostrando los contadores por "507(1)", "CFM(1)", etc.
  Las vistas de terminales y manguitos no cambian; si en ese Excel las
  columnas J, K y O están vacías, esas vistas quedarán vacías (correcto).
================================================================================
2026-07-08 (soporte encabezado multi-fila en Excel)
PROMPT:
Al cargar un Excel con encabezado de más de una fila, la fila de cabecera
aparecía en la tabla y la tarjeta DATOS quedaba vacía. El mapeo de metadatos
para este formato es: EQUIPO=D3, DESCRIPCIÓN=D2, PLANO=D5, LISTA=J4,
EDICIÓN=J2, FECHA=P2. La primera fila de datos siempre empieza por 001..n.

SOLUCIÓN:
- js/app.js, handleFileUpload():
  · Nueva constante _isNumPos: detecta si el valor de col. A es un número
    puro (/^\d+$/), lo que identifica una fila de datos real.
  · _firstDataIdx: índice de la primera fila con posición numérica.
  · _multiHdr = true cuando _firstDataIdx >= 3 (encabezado de varias filas).
  · Si _multiHdr: se usa el mapeo alternativo con celdas absolutas
    json[2]['D']=EQUIPO, json[1]['D']=DESC, json[4]['D']=PLANO,
    json[3]['J']=LISTA, json[1]['J']=EDICIÓN, json[1]['P']=FECHA.
  · Si no: se mantiene el mapeo original (filas Q-V de json[1]).
  · rawData ahora usa .filter(r => _isNumPos(r.posicion)) en lugar de
    .filter(r => r.posicion), excluyendo automáticamente cualquier fila
    de encabezado independientemente de cuántas haya.
================================================================================
2026-07-08 (validación duplicados al cargar Excel)
PROMPT:
Cada vez que se cargue un fichero Excel debe comprobarse que no hay campos
duplicados en las dos primeras columnas. Si hay alguna celda duplicada se
mostrará un mensaje indicando cuál es.

SOLUCIÓN:
- js/app.js: nueva función _checkDuplicateColumns(data) añadida justo antes
  de handleFileUpload. Recorre rawData buscando valores repetidos en los
  campos 'posicion' (col. A) y 'orden' (col. B). Si detecta duplicados,
  construye un mensaje listando los valores afectados por columna y lo
  muestra con window.alert() tras un setTimeout de 200 ms (para que la UI
  termine de renderizarse antes de que aparezca el diálogo).
  La carga del fichero continúa con normalidad; el mensaje es solo un aviso.
  Además, las celdas duplicadas quedan marcadas con fondo rojo parpadeante
  (clase CSS cell-dup-blink / animación dupCellBlink en app.css).
  Para ello se mantienen dos Sets de módulo (_dupPosicions, _dupOrdenes) que
  _checkDuplicateColumns rellena en cada carga, y renderTable los consulta al
  generar cada celda de las columnas posicion y orden.
================================================================================
2026-07-08 (filtro multi-valor con "+")
PROMPT:
Al filtrar usando la caja ELEMENTO o CABLE quiero que añadas la opción de que
al poner el signo "+" filtre por ambos valores. Ejemplo: si escribo X10 + X11
el filtrado se hará por los elementos X10 y X11.

SOLUCIÓN:
- js/app.js: en renderTable(), los filtros de ELEMENTO y CABLE/MARCA se
  parsean por "+" y se aplica lógica OR con Array.some().
  · ELEMENTO: coincidencia exacta (===) en de_elemento / para_elemento
    contra cada término del split.
  · CABLE/MARCA: coincidencia parcial (includes) en cable_marca contra cada
    término del split.
  · Sidebar de materiales: también actualizado para agregar materiales de
    todos los elementos filtrados simultáneamente.
- Los botones de "Acciones rápidas" (detalle, gráfico, marcar completado)
  se ocultan automáticamente cuando el filtro contiene "+", ya que esas
  acciones solo tienen sentido para un único elemento.
================================================================================
2026-07-08
PROMPT:
Implementar gestión de versión semántica (MAYOR.MEJORA.CORRECCIÓN) en el
título de la aplicación. Versión actual: 0.4.0.

SOLUCIÓN:
- js/app.js: constante APP_VERSION = '0.4.0' al inicio del fichero, con
  comentario explicativo del formato de versión. En window.onload, primera
  instrucción: document.title = `ICG Vision – V.${APP_VERSION}`.
- index.html: <title> actualizado a "ICG Vision – V.0.4.0" como fallback
  estático (antes del JS).
Flujo: para cada nueva versión solo hay que cambiar APP_VERSION en app.js.
================================================================================
2026-07-08
PROMPT:
Cambiar el color del icono de exportación TXT a #0424da.

SOLUCIÓN:
- index.html: se reemplaza la clase text-emerald-500/hover:bg-emerald-500/10
  por style="color:#0424da" y onmouseover/onmouseout equivalentes con el
  mismo color en el fondo hover (#0424da1a).
================================================================================
2026-07-08
PROMPT:
Revisar la exportación TXT porque la columna L (marca / para_manguito)
no se estaba exportando.

SOLUCIÓN:
- js/app.js, exportConnectionListToTxt(): se inserta 'para_manguito' entre
  'de_manguito' y 'para_elemento' en TXT_KEYS, completando las 16 columnas
  A–P correctamente y evitando el desplazamiento incorrecto de M–P.
================================================================================
2026-07-08
PROMPT:
Ampliar la exportación TXT hasta la columna P (observaciones).

SOLUCIÓN:
- js/app.js, exportConnectionListToTxt(): se añade 'observaciones' al final
  del array TXT_KEYS, pasando de 14 a 15 columnas (A-P).
================================================================================
2026-07-08
PROMPT:
En la exportación TXT, dejar en blanco las celdas de de_terminal, de_manguito
y para_terminal cuando su valor sea S/T o S/M.

SOLUCIÓN:
- js/app.js, función exportConnectionListToTxt(): se añade el objeto
  TXT_BLANK_IF que mapea las tres columnas a los valores ['S/T','S/M'].
  Durante el mapeo de cada celda, si la clave está en TXT_BLANK_IF y el valor
  coincide, se exporta cadena vacía en lugar del valor original.
================================================================================
2026-07-08
PROMPT:
Añade un icono al encabezado que exporte la vista listado como TXT delimitado
por tabulaciones, sin encabezados, columnas A-O, nombre EQUIPO-LISTA.txt,
guardado como descarga del navegador igual que el Excel.

SOLUCIÓN:
- js/app.js: nueva función exportConnectionListToTxt().
    · Columnas exportadas (A-O): posicion, orden, cod_cable, seccion, longitud,
      marcado, cable_marca, de_elemento, de_punto, de_terminal, de_manguito,
      para_elemento, para_punto, para_terminal.
    · Aplica correcciones de errorsMap igual que la vista de tabla.
    · Sin encabezados, filas separadas por \r\n, campos por \t.
    · Nombre de fichero: EQUIPO-LISTA.txt (caracteres inválidos → '_').
    · Descarga con Blob + <a download> (mismo mecanismo que XLSX.writeFile).
    · Notificación 'success' al completar.
- index.html: botón <file-down> color esmeralda junto al botón Guardar,
  title="EXPORTAR LISTA CONEXIONES (.TXT)", onclick=exportConnectionListToTxt().
================================================================================
2026-07-08
PROMPT:
Quiero que el icono que has añadido parpadee para que llame la atención.

SOLUCIÓN:
- En css/app.css se añadió el keyframe `termWarnBlink` (opacidad 1→0.25 +
  escala 1→0.8 + drop-shadow rojo, ciclo 1 s ease-in-out infinite) y la
  clase `.term-warn-blink` que lo aplica.
- En js/app.js se añade `term-warn-blink` al icono ⚠ del encabezado solo
  cuando el filtro no está activo (estado rojo). En estado activo (amarillo)
  el icono queda fijo para indicar que el filtro ya está seleccionado.
================================================================================
2026-07-08
PROMPT:
Actualmente si en la columna "De terminal" o "Para terminal" se encuentra un
código que no es el apropiado para la sección del cable, ese terminal se marca
de color rojo y se le añade un icono de warning. Quiero que el icono de warning
aparezca en el encabezado cada vez que exista un error de este tipo. Al hacer
clic sobre el icono, se filtrará el informe por los registros que contienen ese
error.

SOLUCIÓN:
Se han realizado tres cambios en js/app.js:

1. Nueva variable de estado `filterTerminalError = null` junto a los demás
   filtros (filterText, filterMarcaText). Puede valer 'de_terminal',
   'para_terminal' o null.

2. Reestructuración de renderTable():
   - El filtrado por elemento y marca se desplaza ANTES de generar la cabecera
     para poder hacer un pre-scan sobre los datos ya filtrados.
   - Pre-scan con _hasTermError(key): comprueba si alguna fila del conjunto
     filtrado tiene una incompatibilidad de sección en la columna indicada,
     usando checkSectionCompatibility().
   - En la generación del <th> de las columnas 'de_terminal' y 'para_terminal',
     si hay errores se inyecta un <i data-lucide="triangle-alert"> clicable:
       · Color rojo (text-red-300) cuando el filtro NO está activo.
       · Color amarillo (text-yellow-300) cuando el filtro SÍ está activo.
       · onclick llama a toggleTerminalErrorFilter(key), que no propaga el
         evento al <th> (event.stopPropagation()) para no activar el ordenado.
   - Tras la cabecera, si filterTerminalError está activo, se filtra d para
     mostrar solo las filas con error en esa columna.

3. Nueva función toggleTerminalErrorFilter(key): activa o desactiva el filtro
   según si ya estaba activo para esa columna; llama a renderTable() y
   lucide.createIcons() para actualizar los iconos SVG.
================================================================================
================================================================================
2026-07-08 11:29
PROMPT:
Al guardar el archivo .txt, cuando los campos EQUIPO y LISTA están vacíos,
no se elimina el encabezado. Cuando ambos tienen valor sí funciona.
Todo lo anterior a la primera fila con posicion '001' debe eliminarse.

SOLUCIÓN:
- js/app.js: en exportConnectionListToTxt(), antes del .map() de líneas,
  se localiza el índice de la primera fila con posicion === '001' mediante
  findIndex. Se exporta solo rawData.slice(firstDataIdx) en lugar del array
  completo. Así, cualquier fila de cabecera del Excel (p.ej. 'Pos.') que haya
  entrado en rawData porque la columna A tenía valor queda excluida del TXT.
================================================================================
================================================================================
2026-07-08 11:51
PROMPT:
Añadir la versión (constante VERSION) en tres sitios:
1) Landing page: centrado justo debajo de ICG VISION.
2) Encabezado: a la derecha de ICG VISION alineado en la zona inferior.
3) Encabezado del modal de ayuda.

SOLUCIÓN:
- index.html landing: el <p id="landingVersion"> estaba en absolute bottom-5.
  Se elimina ese párrafo y se añade uno nuevo justo tras el </h1>, con clases
  text-center text-white/40 text-[10px] tracking-[0.3em] mb-8. Se reduce mb-3
  del h1 a mb-1 para ajustar espaciado.
- index.html encabezado: se añade self-end y leading-none al
  <span id="headerVersion"> para que quede alineado con la zona inferior del
  texto ICG VISION dentro del flex items-baseline padre.
- index.html modal ayuda: <span id="helpModalVersion"> ya estaba correctamente
  colocado tras "Manual de Usuario". Sin cambios necesarios.
- app.js: sin cambios (ya rellenaba los tres elementos en window.onload).
================================================================================
================================================================================
2026-07-08 11:55
PROMPT:
La versión en el encabezado queda desalineada respecto a ICG VISION.

SOLUCIÓN:
- index.html encabezado: se cambia el contenedor padre de items-baseline a
  items-end. Con items-baseline el fondo del contenedor flex incluía el
  espacio de layout del SVG (w-5 h-5 = 20px) y self-end bajaba la versión
  más de lo visual. Con items-end todos los elementos (ICG, Visi👁n, versión)
  se alinean por su borde inferior real. Se eliminan self-end y leading-none
  del span #headerVersion.
================================================================================
================================================================================
2026-07-08 12:01
PROMPT:
Con items-end la version aun queda un poco alta. Necesita bajar y que su
parte inferior coincida con la parte inferior de ICG VISION.

SOLUCION:
- index.html: se revierte el contenedor a items-baseline (el borde inferior
  visual de letras mayusculas = su linea de base, de modo que items-baseline
  alinea exactamente los fondos visibles de ICG VISION y V.x.x.x).
  Se anade translate-y-[1px] al span #headerVersion para compensar el leve
  desplazamiento que introduce el SVG del ojo al fijar la linea de base del
  contenedor interno mas abajo que la del texto ICG.
================================================================================
================================================================================
2026-07-08 12:18
PROMPT:
Quitar la versión del encabezado. Dejarla solo en landing page y modal ayuda.

SOLUCIÓN:
- index.html: eliminado el <span id="headerVersion"> del encabezado.
- js/app.js: eliminadas las dos líneas que obtenían y rellenaban headerVersion.
================================================================================
================================================================================
2026-07-08 12:50
PROMPT:
Cuando la lista de conexiones del popover es muy grande, hay que poder hacer
scroll para ver la parte oculta.

SOLUCIÓN:
- index.html, #pinPopover: añadidos flex flex-col y max-h-[80vh] para limitar
  la altura máxima al 80% de la pantalla y max-w-[340px] para uniformidad.
- Cabecera (pin name + botón X): shrink-0 para que no se comprima.
- #popoverContent: overflow-y-auto custom-scrollbar flex-1 pr-1 para que el
  área de contenido ocupe el espacio disponible y habilite el scroll vertical
  cuando el contenido supera la altura máxima.
================================================================================
================================================================================
2026-07-08 12:58
PROMPT:
En pantalla completa con tablet/smartphone, permitir pinch-to-zoom y pan para
ver contenido oculto del diagrama SVG.

SOLUCIÓN:
- js/app.js, handlers touch del SVG (#diagramSvg):
  1. touchstart: se añade e.preventDefault() para impedir que iOS Safari
     intercepte el gesto de pinch antes que el JS. Se almacena el punto
     medio (cx,cy) entre los dos dedos en lastTouchX/Y al iniciar pinch.
  2. touchmove (2 dedos): además del zoom proporcional a la distancia entre
     dedos, ahora también actualiza panX/panY con el desplazamiento del punto
     medio. Así se puede pellizcar y mover el diagrama simultáneamente.
  3. touchend: si queda 1 dedo al levantar el segundo, se reactiva el pan
     desde la posición actual de ese dedo (evita salto brusco).
================================================================================
================================================================================
2026-07-10
PROMPT:
Añadir un control para que si en algún fichero Excel llega en alguna celda de
la columna E algún carácter que no sea numérico, muestre una advertencia y lo
marque como error tal y como se hace para las posiciones duplicadas o los
terminales no compatibles con la sección.

SOLUCIÓN:
- js/app.js: nuevo Set global _nonNumericLongitud (junto a _dupPosicions y
  _dupOrdenes) que almacena las posiciones con valor no numérico en longitud.
- js/app.js, _checkDuplicateColumns(data):
  · Se limpia _nonNumericLongitud.clear() al inicio junto a los otros Sets.
  · Se detectan filas con r.longitud no vacío y cuyo valor no coincide con
    /^[\d.,]+$/ (números, coma decimal y punto). Las posiciones afectadas
    se añaden al Set.
  · La condición de salida temprana incluye _nonNumericLongitud.size.
  · El mensaje de alerta añade una línea "● Longitud con valor no numérico
    (col. E) (N): pos. X, Y, Z..." con conteo y lista de posiciones.
- js/app.js, renderTable():
  · Pre-scan: hasNonNumericLongitud = _nonNumericLongitud.size > 0.
  · Cabecera: colHasNonNumLong = c.key === 'longitud' && hasNonNumericLongitud
    → se incluye en colHasAnyError y genera icono triangle-alert parpadeante
    con tooltip "Hay longitudes con caracteres no numéricos — clic para filtrar".
  · Filtrado (paso 2): nuevo caso filterTerminalError === 'longitud' filtra
    filas cuya posición está en _nonNumericLongitud.
  · Celda: hasNonNumLong marca la celda con fondo rojo (bg-red-50), texto rojo
    y un icono triangle-alert con tooltip "Valor no numérico en longitud (col. E)".
================================================================================
2026-07-08 13:06
PROMPT:
En el modo esquema grafico dejo de funcionar toda la parte interactiva: no
ocurre nada al pulsar pines ni al navegar pulsando un elemento.

CAUSA:
El e.preventDefault() añadido en touchstart para todos los toques suprimia
la generacion de eventos click sinteticos del navegador. Los pines y elementos
del diagrama usan onclick, que depende de ese click sintetico.

SOLUCION:
- js/app.js, touchstart del SVG: e.preventDefault() solo cuando
  e.touches.length >= 2 (gesto de pinch). Para 1 dedo NO se llama
  preventDefault(), de modo que los taps siguen disparando onclick
  normalmente. El pan de 1 dedo sigue funcionando via touchmove
  (que si llama preventDefault para evitar el scroll del navegador).
================================================================================
